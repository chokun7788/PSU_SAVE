# Daily Log - 2026-07-17

## PSU Esports Chatbot

- อัปเดต `18_PSU_Esports_Update_Route_Data/notebooks/04_local_hybrid_chat_debug.ipynb`
- เพิ่ม cell ท้าย notebook สำหรับเรียก `chat_loop()` โดยตรง เพื่อให้กด Run แล้วเริ่มแชท local ได้ทันที
- เพิ่ม `.gitignore` ในโฟลเดอร์ 18 ให้ ignore `reports/local_hybrid_chat_debug/` เพื่อกันเผลอ commit log/session chat local

## Usage Note

- รัน cell ตั้งค่า, cell เช็ก Ollama, cell helper, แล้วรัน cell ท้ายสุด `chat_loop()`
- ใช้ `/exit` เพื่อ save snapshot และออกจาก session

## Local LLM General Fallback

- ปรับ general fallback ไม่ให้ขึ้นข้อความแนว `คำถามนี้อยู่นอกฐานข้อมูล PSU...` ก่อนตอบ เพราะทำให้ดูเหมือน rule/no-answer ดักก่อนถึง Local LLM
- ปรับ prompt สำหรับคำถามทั่วไปให้ส่งเข้า Local LLM แบบตรงกว่าเดิม โดยให้ตอบจากความรู้ทั่วไปของโมเดล
- ปรับข้อความ fallback ตอน LLM ใช้ไม่ได้ให้บอกเฉพาะว่า Local LLM ไม่ส่งคำตอบ/ยังไม่ได้เปิด ไม่ใช่บอกว่าคำถามอยู่นอกฐาน PSU
- เพิ่ม `GENERAL_NUM_PREDICT` ใน notebook เป็น `1024` เพื่อช่วยกรณี Qwen3 ใช้ token กับ thinking แล้ว response ว่าง
- ล้าง output เก่าใน notebook เพื่อไม่ให้สับสนกับผลรันก่อนหน้า
- เพิ่ม troubleshooting note ใน notebook สำหรับ `general_llm_unavailable`
- เพิ่มการบังคับอัปเดตค่า `experimental_fallback.DEFAULT_MODEL`, timeout และ num_predict หลัง rerun cell ตั้งค่า เพื่อกันค่า model/timeout ค้างใน kernel เดิม
- แก้กรณี `qwen3:4b` ตอบ `general_llm_unavailable` เพราะคิดจน `num_predict` หมด โดยส่ง `think: false` ให้ Ollama ใน general fallback
- อัปเดต cell เช็ก Ollama ใน notebook ให้ใช้ `think: false` เหมือน pipeline จริง เพื่อลดเคส response ว่างจาก thinking model
- ทดสอบเทียบ `qwen3:4b` กับ `qwen2.5:3b` พบว่า `qwen3:4b` ยังชอบส่งข้อความแนว reasoning ใน response แม้ `think: false` แล้ว จึงปรับ notebook default กลับเป็น `qwen2.5:3b`
- เพิ่ม `PSU_OLLAMA_THINK=false` ใน `start_local_ai_chat.ps1` ให้โหมด terminal chat ปิด thinking ชัดเจนเหมือน notebook/pipeline
- แก้ UX ใน `04_local_hybrid_chat_debug.ipynb` ที่ `input()` ใน `chat_loop()` ดูเหมือนค้างหลังถามข้อแรก โดยเพิ่ม `start_widget_chat()` สำหรับถามต่อเนื่องผ่านช่องพิมพ์/ปุ่มใน notebook
- ปรับ `chat_loop()` ให้ซ่อน trace เป็นค่า default และพิมพ์ `พร้อมถามต่อ` หลังตอบ เพื่อให้เห็นชัดว่ารับคำถามถัดไปได้
- ปรับ cell ท้ายสุดของ notebook เป็นรูปแบบ `question = "..."; ask(...)` ให้ผู้ใช้แก้ข้อความคำถามเองแล้วกด Run ซ้ำได้ตรง ๆ โดยไม่ต้องใช้ loop/widget
- เพิ่ม `new_session()` ใน notebook และปรับ `ask()` ให้เปิด session ใหม่อัตโนมัติถ้า kernel ยังมีสถานะ `closed=True` จากการ `/exit`
- แก้ routing/fast answer สำหรับคำถาม PS5 ราคาและ booking limit: คำถามราคาแบบ `9โมงถึง11โมง` จะเข้า service fee calculator, คำถาม `เล่นได้กี่ชั่วโมงต่อวัน` จะเข้า booking session limit แทน schedule
- เพิ่ม parser ช่วงเวลาใน price fast path เพื่อคำนวณ session รวม เช่น PS5 09:00-11:00 = 2 sessions และเช็กช่วง Maintenance วันจันทร์เช้า/ศุกร์บ่ายก่อนตอบราคา
- เพิ่ม `tests/smoke_test_booking_price_regression.py` ครอบคลุมเคส `PS5 เล่นได้กี่ชั่วโมงต่อวัน`, `คนนึงเล่นได้กี่ชั่วโมงต่อวัน`, `ถ้าจอง PS5 9โมงถึง11โมงเสียกี่บาท`, และ `วันจัทร์ PS5 9โมงถึง12โมงเสียกี่บาท`
- ปรับคำตอบช่วงเวลา 09:00-12:00 ให้แสดงการแบ่ง session รายชั่วโมงก่อน เช่น 09:00-10:00, 10:00-11:00, 11:00-12:00 แล้วค่อยใส่หมายเหตุเรื่อง Maintenance หากวันนั้นจองจริงไม่ได้
