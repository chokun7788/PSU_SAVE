# 09 — RAG ภาษาไทยโดยเฉพาะ (จุดที่ต้องระวังเป็นพิเศษ) + โค้ดตัวอย่าง

> ทำไมต้องมีไฟล์นี้: tutorial ส่วนใหญ่เขียนสำหรับอังกฤษ ถ้าเอาค่า default ฝรั่งมาใช้กับเอกสารไทยตรงๆ คุณภาพมักตกโดยไม่รู้ตัว ไฟล์นี้รวมจุดที่ "ไทยต่างจากอังกฤษ" และต้องจัดการพิเศษ

---

## 9.1 การตัดคำภาษาไทย (Thai Word Segmentation)

### คืออะไร / ทำไมสำคัญ
ภาษาไทย **ไม่มีเว้นวรรคระหว่างคำ** ("ไปกินข้าว" ไม่ใช่ "ไป กิน ข้าว") เครื่องมือฝรั่งที่ตัดตามช่องว่าง/ตัวอักษรจะตัดมั่ว ทำให้ chunking และ keyword search เพี้ยน

### ต้องรู้อะไรบ้าง + โค้ด
```python
# pip install pythainlp
from pythainlp.tokenize import word_tokenize

text = "ผมกำลังเรียนเรื่องการสร้างระบบค้นหาเอกสารด้วยปัญญาประดิษฐ์"
print(word_tokenize(text, engine="newmm"))
# ['ผม','กำลัง','เรียน','เรื่อง','การ','สร้าง','ระบบ','ค้นหา','เอกสาร','ด้วย','ปัญญาประดิษฐ์']
```
```python
# ใช้ตัวตัดคำไทยช่วยตอน chunking (ตัดที่ขอบเขตคำ ไม่ตัดกลางคำ)
def thai_safe_chunks(text, max_chars=400):
    words, chunks, buf = word_tokenize(text, engine="newmm"), [], ""
    for w in words:
        if len(buf) + len(w) > max_chars:
            chunks.append(buf); buf = ""
        buf += w
    if buf: chunks.append(buf)
    return chunks
```

- **PyThaiNLP**: ไลบรารี NLP ไทยหลัก มีตัวตัดคำหลาย engine (newmm, attacut, deepcut)
- ใช้ตอน **chunking** (ไม่ตัดกลางคำ) และ **keyword/BM25 search** (ต้องตัดคำก่อน)
- การเว้นวรรคในไทยคั่นวลี/ประโยค ไม่ใช่คั่นคำ — ระวังตอนแบ่งประโยค

### ศึกษายังไง / วิธี
1. ลอง `word_tokenize` กับข้อความไทยของคุณ ดูว่าตัดถูกไหม (ลองหลาย engine)
2. เทียบ chunking แบบใช้/ไม่ใช้ตัวตัดคำไทย แล้ววัดผล (เฟส 5)

---

## 9.2 Tokenization กับ Cost ของภาษาไทย

### คืออะไร / ทำไมสำคัญ
LLM ส่วนใหญ่ tokenize ไทยได้ "ไม่ประหยัด" — ข้อความไทยมักกลายเป็น token เยอะกว่าอังกฤษที่ความหมายเท่ากัน

### ต้องรู้อะไรบ้าง + โค้ด
```python
import tiktoken
enc = tiktoken.get_encoding("cl100k_base")
th = "ระบบนี้ช่วยให้พนักงานค้นหาข้อมูลในเอกสารบริษัทได้อย่างรวดเร็ว"
en = "This system helps employees quickly find information in company documents"
print("ไทย:", len(enc.encode(th)), "tokens")     # มักมากกว่า
print("อังกฤษ:", len(enc.encode(en)), "tokens")  # น้อยกว่า ทั้งที่ความหมายพอกัน
```
- ข้อความไทยเท่ากันอาจกิน token มากกว่าหลายเท่า → **context เต็มเร็วกว่า + ค่าใช้จ่ายสูงกว่า**
- เผื่อ context budget และประมาณ cost ใหม่สำหรับงานไทย
- เลือกโมเดลที่ tokenizer รองรับไทยดี (โมเดลที่เทรนไทยมักประหยัด token ไทยกว่า)

### ศึกษายังไง / วิธี
1. เอาข้อความไทยจริงไปลองใน tokenizer ของโมเดลที่จะใช้ นับ token
2. คำนวณ cost ต่อคำถามด้วยข้อความไทยจริง อย่าประมาณจากตัวเลขอังกฤษ

---

## 9.3 Embedding Model สำหรับภาษาไทย

### คืออะไร / ทำไมสำคัญ
embedding ที่เทรนอังกฤษล้วน จับความหมายไทยได้แย่ → semantic search ไทยพลาด

### ต้องรู้อะไรบ้าง / มีตัวเลือกอะไร + โค้ดเทียบ
```python
from sentence_transformers import SentenceTransformer
import numpy as np
def cos(a, b): return np.dot(a, b)/(np.linalg.norm(a)*np.linalg.norm(b))

# เทียบหลายโมเดลบนคู่ไทยของคุณเอง แล้วเลือกตัวที่แยกความหมายได้ดีสุด
for name in ["intfloat/multilingual-e5-small", "BAAI/bge-m3"]:
    m = SentenceTransformer(name)
    v = m.encode(["ลาพักร้อน", "วันหยุดพักผ่อนประจำปี", "ค่าเดินทาง"])
    print(name)
    print("  ลาพักร้อน vs วันหยุด:", round(cos(v[0], v[1]), 3))  # ควรสูง (ความหมายใกล้)
    print("  ลาพักร้อน vs ค่าเดินทาง:", round(cos(v[0], v[2]), 3))  # ควรต่ำ (ไม่เกี่ยว)
```

- ต้องใช้ **multilingual embedding** หรือโมเดลที่เทรน/รองรับไทย เช่น
  - ตระกูล **multilingual-e5** (small/base/large)
  - **BGE-M3** (multilingual, รองรับ dense + sparse ในตัว — ดีต่อ hybrid)
  - โมเดลไทย/multilingual บน Hugging Face (เช็ก MTEB + ค้นที่ระบุรองรับไทย)
- ทดสอบกับข้อมูล "ของคุณเอง" เสมอ — leaderboard เป็นแค่จุดเริ่ม
- reranker ก็ต้องรองรับไทย (BGE-reranker, Jina multilingual)

### ศึกษายังไง / วิธี
1. คัด 2–3 โมเดล วัด context recall/precision บน test set ไทยของคุณ เลือกตัวดีสุด
2. ลอง **BGE-M3** ถ้าจะทำ hybrid (ได้ทั้ง dense+sparse จากโมเดลเดียว)

---

## 9.4 LLM ที่เข้าใจภาษาไทยดี

### คืออะไร / ทำไมสำคัญ
ขั้น generation ถ้า LLM เข้าใจไทยไม่ดี คำตอบจะแข็ง ผิดไวยากรณ์ หรือเข้าใจคำถามผิด

### ต้องรู้อะไรบ้าง + โค้ด
```python
# Typhoon (SCB10X) — LLM ที่พัฒนาเน้นภาษาไทย, API เข้ากันได้กับ OpenAI
from openai import OpenAI
client = OpenAI(base_url="https://api.opentyphoon.ai/v1", api_key="YOUR_TYPHOON_KEY")
r = client.chat.completions.create(
    model="typhoon-v2-70b-instruct",   # ตรวจชื่อรุ่นล่าสุดจากเอกสาร Typhoon ณ วันใช้งาน
    messages=[{"role": "user", "content": "สรุปนโยบายการลาเป็นภาษาไทยกระชับ"}],
)
print(r.choices[0].message.content)
```
- **Typhoon** (โดย SCB10X): ตระกูล LLM เน้นไทย — ตัวเลือกเด่นสำหรับงานไทย
- โมเดล cloud ใหญ่ (Claude, GPT, Gemini) ทำงานไทยได้ดีขึ้นมากแล้ว — เทียบกับงานจริง
- โมเดล open-source multilingual (Qwen, Llama, Gemma) บางตัวไทยใช้ได้ ลองเทียบ
- **ประเมินด้วยงานจริง**: วัดบน test set ไทยของคุณเอง

### ศึกษายังไง / วิธี
1. รันคำถามไทยชุดเดียวกันผ่านหลายโมเดล เทียบคุณภาพ + cost + latency
2. ติดตามงานของ **SCB10X** (Typhoon) และชุมชน AI ไทย

---

## 9.5 ลักษณะเฉพาะของเอกสาร/คำถามภาษาไทย

### ต้องรู้อะไรบ้าง
- **คำสะกดหลากหลาย/คำทับศัพท์**: คำเดียวสะกดหลายแบบ, อังกฤษปนไทย — **hybrid search** ช่วยมาก (ไฟล์ `05`)
- **ภาษาพูด vs ภาษาเขียน**: คำถามผู้ใช้มักเป็นภาษาพูด แต่เอกสารเป็นทางการ → **query transformation** ช่วย bridge
- **เอกสารราชการ/ฟอร์มไทย**: มักเป็น PDF สแกน มีตาราง → ต้อง OCR + parser ดีๆ
- **ตัวเลขไทย/พ.ศ. vs ค.ศ.**: ระวังเรื่องปี และเลขไทยตอน normalize

```python
# normalize ปี พ.ศ. -> ค.ศ. และเลขไทย -> อารบิก (ตัวอย่างเบื้องต้น)
from pythainlp.util import thai_digit_to_arabic_digit
def normalize(text):
    text = thai_digit_to_arabic_digit(text)   # ๒๕๖๗ -> 2567
    return text
```

### ศึกษายังไง / วิธี
1. ใส่ hybrid search ตั้งแต่ต้นถ้าทำงานไทย (คุ้มกว่ารอ)
2. รวบรวมคำถามไทยจริงจากผู้ใช้มาทำ test set — สะท้อนภาษาพูดจริง

---

## 9.6 PDPA และความเป็นส่วนตัว (กฎหมายไทย)

### คืออะไร / ทำไมสำคัญ
**PDPA (พ.ร.บ.คุ้มครองข้อมูลส่วนบุคคล)** คือกฎหมายไทยเรื่องข้อมูลส่วนบุคคล ถ้าระบบ RAG ประมวลผลข้อมูลคนจริง ต้องทำให้ถูกกฎหมาย

> หมายเหตุ: นี่เป็นข้อมูลทั่วไปเพื่อให้รู้ว่าต้องคำนึงถึง ไม่ใช่คำแนะนำทางกฎหมาย การปฏิบัติตามจริงควรปรึกษาผู้เชี่ยวชาญ/ฝ่ายกฎหมายขององค์กร

### ต้องรู้อะไรบ้าง (ระดับตระหนัก)
- ข้อมูลส่วนบุคคล (PII) ในเอกสาร/log อาจอยู่ใต้ PDPA
- ระวังการส่ง PII ออกไป LLM API ภายนอก (พิจารณา local LLM / ปกปิดข้อมูล / ข้อตกลงผู้ให้บริการ)
- ทำ access control ให้ผู้ใช้เห็นเฉพาะข้อมูลที่มีสิทธิ์
- เก็บ log อย่างระวัง (อาจมี PII)

```python
# ตัวอย่างปกปิด PII คร่าวๆ ก่อนส่งออก/เก็บ log (ควรใช้ร่วมกับเครื่องมือ PII detection จริงจัง)
import re
def mask_pii(text):
    text = re.sub(r"\b\d{13}\b", "[เลขบัตรประชาชน]", text)        # เลขบัตร 13 หลัก
    text = re.sub(r"\b0\d{8,9}\b", "[เบอร์โทร]", text)             # เบอร์โทร
    text = re.sub(r"[\w.+-]+@[\w-]+\.[\w.-]+", "[อีเมล]", text)    # อีเมล
    return text
```

### ศึกษายังไง / วิธี
1. ตรวจว่าข้อมูลมี PII ไหม วางแผนจัดการตั้งแต่ออกแบบ
2. ถ้าข้อมูลอ่อนไหวมาก พิจารณา local LLM (Ollama/Typhoon) เพื่อไม่ให้ข้อมูลออกนอกองค์กร
3. ปรึกษาฝ่ายกฎหมาย/ผู้เชี่ยวชาญ PDPA สำหรับการใช้งานจริง

---

## ✅ เช็กลิสต์เฉพาะงานไทย

- [ ] ใช้ตัวตัดคำไทย (PyThaiNLP) ตอน chunking / keyword search
- [ ] วัดและเผื่อ token/cost ด้วยข้อความไทยจริง
- [ ] เลือก embedding model + reranker ที่รองรับไทย โดยวัดบน test set ไทยของตัวเอง
- [ ] เลือก/เทียบ LLM ที่ตอบไทยได้ดี (รวม Typhoon)
- [ ] ใส่ hybrid search สำหรับคำเฉพาะ/ทับศัพท์
- [ ] มีแผนเรื่อง PDPA และ access control ถ้ามีข้อมูลส่วนบุคคล

ไปต่อ `10_แหล่งเรียนและโปรเจค.md`
