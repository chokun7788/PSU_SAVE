# 01 — พื้นฐานที่ต้องมีก่อน (เฟส 0) + โค้ดตัวอย่าง

> เป้าหมายเฟสนี้: มีเครื่องมือและพื้นฐานพอที่จะ "ลงมือทำ RAG ได้" — ไม่ใช่เพื่อเป็นโปรแกรมเมอร์เต็มตัว
> หลักการ: เรียน **พอใช้งาน** แล้วไปต่อ อย่าจมอยู่ที่นี่นานเกิน 6 สัปดาห์

---

## 0.1 Python (สำคัญที่สุดในเฟสนี้)

### คืออะไร
ภาษาโปรแกรมที่เป็นภาษากลางของวงการ AI/ML แทบทุก library และ framework ของ LLM/RAG เขียนด้วย Python หรือมี Python SDK

### ต้องรู้อะไรบ้าง (เช็กลิสต์) + โค้ดที่ต้องเขียนได้

**โครงสร้างข้อมูลหลัก — `dict` และ `list` (ใช้ตลอดเพราะ JSON จาก API มาเป็นแบบนี้)**
```python
# dict = key-value (เหมือน JSON) — เจอบ่อยสุดในงาน LLM
message = {"role": "user", "content": "สวัสดี"}
print(message["role"])          # -> user

# list ของ dict = ประวัติแชต (รูปแบบมาตรฐานของ LLM API)
messages = [
    {"role": "system", "content": "คุณเป็นผู้ช่วยภาษาไทย"},
    {"role": "user", "content": "1+1 เท่ากับเท่าไร"},
]
messages.append({"role": "assistant", "content": "เท่ากับ 2"})  # เพิ่มเข้า list
```

**ฟังก์ชัน + type hint (ช่วยให้โค้ด RAG อ่านง่าย)**
```python
def build_prompt(question: str, context: str) -> str:
    return f"ข้อมูล: {context}\n\nคำถาม: {question}"
```

**List/Dict comprehension (เจอบ่อยมากในโค้ด embedding)**
```python
docs = ["ข้อความ A", "ข้อความ B", "ข้อความ C"]
lengths = [len(d) for d in docs]                  # [8, 8, 8]
indexed = {i: d for i, d in enumerate(docs)}      # {0: 'ข้อความ A', ...}
```

**Error handling (ขาดไม่ได้ตอนเรียก API ที่อาจ fail)**
```python
import time
def call_with_retry(fn, retries=3):
    for attempt in range(retries):
        try:
            return fn()
        except Exception as e:
            print(f"ครั้งที่ {attempt+1} ล้มเหลว: {e}")
            time.sleep(2 ** attempt)   # exponential backoff
    raise RuntimeError("ล้มเหลวทุกครั้ง")
```

**การจัดการไฟล์**
```python
# อ่านไฟล์ข้อความ (พื้นฐานของ document loading)
with open("doc.txt", encoding="utf-8") as f:   # encoding utf-8 สำคัญสำหรับไทย!
    text = f.read()
```

เช็กลิสต์หัวข้อที่ต้องคล่อง: ตัวแปร/ชนิดข้อมูล, `list`/`dict`/`tuple`/`set`, `if/for/while`, ฟังก์ชัน + default argument, `try/except`, `with open`, comprehension, `pip install`, virtual environment, (ไว้ทีหลัง) `async/await`

### สิ่งที่ "ยังไม่ต้องรู้ลึก" ตอนนี้
OOP ขั้นสูง (inheritance, metaclass, decorator ซับซ้อน — รู้แค่ใช้ class เป็นพอ), multithreading/multiprocessing ละเอียด, design pattern

### ศึกษายังไง / วิธี
1. คอร์สพื้นฐานไทย: **MeowCode**, **Ultimate Python**, **Mikelopster**
2. ฟรีระดับโลก: **Python official tutorial** (docs.python.org), **freeCodeCamp** (YouTube คลิป Python 4 ชม.)
3. ฝึกมือ: **Exercism** (track Python) หรือ **HackerRank** วันละ 1–2 ข้อ
4. **โปรเจคจบหัวข้อ**: สคริปต์อ่าน CSV แล้วสรุปข้อมูล หรือเรียก API ฟรีแล้ว print ผล

> 💡 เรียนถึง "ฟังก์ชัน + dict + เรียก API" ก็ขยับไปเฟส 1 ได้ ที่เหลือเก็บระหว่างทาง

---

## 0.2 API และ JSON

### คืออะไร
- **API**: ช่องทางให้โปรแกรมคุยกับบริการอื่น (ส่งคำถามไป LLM รับคำตอบกลับ)
- **REST API**: รูปแบบ API นิยมสุด ใช้ HTTP method
- **JSON**: รูปแบบข้อมูลที่ API รับส่ง หน้าตาเหมือน dict ของ Python

### ต้องรู้อะไรบ้าง + โค้ด

**เรียก REST API ด้วย `requests` (เข้าใจว่า SDK ของ LLM ห่อสิ่งนี้ไว้)**
```python
import requests

resp = requests.get("https://api.github.com/users/torvalds")
print(resp.status_code)        # 200 = สำเร็จ, 401 = key ผิด, 429 = ถี่เกิน, 500 = server พัง
data = resp.json()             # แปลง JSON response เป็น dict อัตโนมัติ
print(data["public_repos"])
```

**POST + headers + body (รูปแบบเดียวกับเวลายิง LLM API ตรงๆ)**
```python
resp = requests.post(
    "https://api.example.com/v1/endpoint",
    headers={"Authorization": "Bearer YOUR_KEY", "Content-Type": "application/json"},
    json={"input": "ข้อความ", "max_tokens": 100},   # body เป็น JSON
)
```

**แปลง JSON ↔ dict**
```python
import json
d = {"name": "RAG", "phase": 3}
s = json.dumps(d, ensure_ascii=False)   # dict -> str (ensure_ascii=False เพื่อให้ไทยอ่านออก)
back = json.loads(s)                     # str -> dict
```

**เก็บ API key ให้ปลอดภัย (ความผิดพลาดอันดับ 1 ของมือใหม่คือ key หลุด)**
```python
# .env  ->  OPENAI_API_KEY=sk-xxxx   และใส่ .env ใน .gitignore เสมอ
import os
from dotenv import load_dotenv
load_dotenv()
api_key = os.getenv("OPENAI_API_KEY")   # อ่านจาก environment ไม่ hardcode ใน code
```

### ศึกษายังไง / วิธี
1. อ่าน concept REST API (ค้น "REST API คืออะไร")
2. เล่น **Postman** / **Thunder Client** (extension ใน VS Code) ยิง API ฟรีดูผล
3. เขียน Python เรียก `https://api.github.com`
4. **โปรเจคจบหัวข้อ**: ยิง API ฟรี รับ JSON แล้วดึงเฉพาะ field ที่ต้องการมา print

---

## 0.3 เครื่องมือพื้นฐาน (Terminal, Git, Environment)

### คืออะไร
ชุดเครื่องมือที่ใช้ทำงานจริงทุกวัน ไม่ใช่ตัว AI แต่ขาดไม่ได้

### ต้องรู้อะไรบ้าง + คำสั่งจริง

**Terminal**
```bash
cd my-project          # เข้าโฟลเดอร์
ls          # (Windows: dir) ดูไฟล์
mkdir data             # สร้างโฟลเดอร์
python app.py          # รันสคริปต์
```

**Virtual environment (แยกสภาพแวดล้อมต่อโปรเจค กันชนกันของ version)**
```bash
python -m venv .venv
source .venv/bin/activate     # Windows: .venv\Scripts\activate
pip install -r requirements.txt
pip freeze > requirements.txt # บันทึก dependency ที่ใช้
```

**Git + GitHub**
```bash
git init
git add .
git commit -m "phase 0: hello rag"
git branch -M main
git remote add origin https://github.com/you/repo.git
git push -u origin main
```

### ศึกษายังไง / วิธี
1. Git: เล่น **learngitbranching.js.org** (interactive ฟรี เห็นภาพ branch)
2. Terminal: ค้น "command line basics" ทำตาม 30 นาที
3. **Jupyter / Google Colab**: รันโค้ดทีละเซลล์ เหมาะทดลอง AI — Colab ฟรีและมี GPU
4. **นิสัยที่ควรมี**: สร้าง repo ใหม่ทุกโปรเจค commit บ่อยๆ

---

## 0.4 คณิตศาสตร์ที่จำเป็น (ระดับ intuition พอ)

### คืออะไร
พื้นฐานคณิตที่ทำให้ "เข้าใจว่าทำไม" embedding/similarity ทำงาน — ไม่ใช่เพื่อคำนวณเอง

### ต้องรู้อะไรบ้าง + โค้ดให้เห็นภาพ

**เวกเตอร์ และ cosine similarity (หัวใจของ semantic search — ลองคำนวณเองสัก 1 ครั้ง)**
```python
import numpy as np

def cosine_similarity(a, b):
    a, b = np.array(a), np.array(b)
    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))

dog   = [0.9, 0.1, 0.0]   # สมมติเวกเตอร์ของ "หมา"
puppy = [0.8, 0.2, 0.0]   # "ลูกหมา"
car   = [0.0, 0.1, 0.9]   # "รถยนต์"

print(cosine_similarity(dog, puppy))  # ~0.98  ใกล้กันมาก (ความหมายคล้าย)
print(cosine_similarity(dog, car))    # ~0.06  ไกลกัน (ความหมายต่าง)
```
นี่คือหลักทั้งหมดของการค้นแบบ semantic: **เวกเตอร์ใกล้กัน = ความหมายใกล้กัน**

ต้องเข้าใจ (แค่ระดับความหมาย): เวกเตอร์คืออะไร, similarity (cosine / dot product / euclidean), ความน่าจะเป็นเบื้องต้น (LLM ทำนาย "คำถัดไปที่น่าจะเป็นที่สุด")

### สิ่งที่ "ยังไม่ต้องเรียน" ตอนนี้
Calculus, การพิสูจน์ทฤษฎี, linear algebra เต็มคอร์ส, backpropagation — จำเป็นเฉพาะถ้าจะ **เทรน/fine-tune โมเดลเอง** (เฟส 7) ไม่ใช่สร้าง RAG application

### ศึกษายังไง / วิธี
1. ดู **3Blue1Brown** "Essence of Linear Algebra" (มีซับไทย) — เห็นภาพเวกเตอร์สวยมาก
2. ค้น "cosine similarity อธิบาย" ภาษาไทย 1–2 คลิป
3. ปูพื้น ML/DL จริงจัง (optional): **Andrew Ng** (Coursera) หรือ **Skooldio Intro to Deep Learning** (ฟรี ไทย) — **ไม่จำเป็น** ต่อการทำ RAG ทำทีหลังได้

> 💡 ความเชื่อผิดที่ทำให้คนเลิกกลางทางคือ "ต้องเก่งคณิตก่อนถึงทำ AI ได้" — สำหรับ RAG application คณิตระดับ intuition พอจริงๆ

---

## ✅ เช็กลิสต์ก่อนขึ้นเฟส 1

- [ ] เขียนฟังก์ชัน Python ที่รับ input คืน output ได้เอง
- [ ] ใช้ `dict` และ `list` คล่อง (โดยเฉพาะ list ของ dict)
- [ ] เรียก API ฟรีด้วย `requests` แล้วอ่าน JSON ที่ได้กลับมา
- [ ] เก็บ API key ใน `.env` + ใส่ `.env` ใน `.gitignore`
- [ ] สร้าง virtual environment + ติดตั้ง package เป็น
- [ ] push โค้ดขึ้น GitHub เป็น
- [ ] เขียนฟังก์ชัน cosine similarity เองและอธิบายได้ว่า "เวกเตอร์ใกล้กัน = ความหมายใกล้กัน"

ครบแล้วไปต่อ `02_LLM_พื้นฐาน.md`
