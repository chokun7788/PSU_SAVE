# 10 — รวมแหล่งเรียน + โปรเจคแต่ละเฟส + คำศัพท์ (ไฟล์อ้างอิง ใช้ตลอดทาง)

> ใช้ไฟล์นี้เป็น "สมุดหน้าเหลือง" เปิดดูเมื่อต้องการแหล่งเรียนของเฟสที่กำลังทำ หรือหาโปรเจคทำจบเฟส

---

## 🇹🇭 แหล่งเรียนภาษาไทย

### พื้นฐาน Python / Programming (เฟส 0)
- **MeowCode**, **Ultimate Python** — Python พื้นฐานสำหรับมือใหม่
- **Mikelopster** (mikelopster.dev) — เนื้อหา dev กว้าง รวม AI/LLM
- **DataTH** — สาย Data/AI ภาษาไทย เข้าใจง่าย

### LLM / RAG โดยเฉพาะ
- **VulturePrime** — "RAG 101 Step by Step" (อธิบาย query เทียบกับ vector DB)
- **Nextflow (nextflow.in.th)** — บทความแนะนำ RAG
- **Orapin Anonthanasap (Medium)** — advanced RAG, embeddings/DPR เชิงลึก
- **Appsynth (blog)** — Llama 3.1 + LangChain + RAG + FAISS (ลงมือจริง)
- **codingthailand** — LangChain.js + RAG + Next.js + streaming (สาย web/JS)
- **SCB10X (blog/ช่องทางทางการ)** — ผู้สร้าง Typhoon (LLM ไทย), เบื้องหลัง LLM
- **Mikelopster** — "LLM Local and API"

### คอร์ส / แพลตฟอร์มไทย
- **Skooldio** — Intro to Deep Learning (ฟรี), คอร์ส Data/AI
- **SkillLane** — LLMs 101, LangChain + Python, AI Agent
- **IMC Institute** — Advanced RAG, deploy ด้วย Docker (เวิร์กชอป ~5,900–6,900 บาท)
- **Cloudnone** — Docker / Kubernetes / cloud เป็นภาษาไทย (เฟส 6)
- **Disrupt**, **TNIC** — เนื้อหา/คอร์ส AI เชิงธุรกิจและเทคนิค

### ชุมชน
- กลุ่ม/เพจ AI ไทย เช่น AI Builders, AI Thailand, กลุ่ม ML/Data ต่างๆ — ถามปัญหา ติดตามข่าว หาเพื่อนเรียน

> 💡 แหล่งไทยดีสำหรับ "เข้าใจ concept เร็ว" แต่ของล่าสุด/ลึกสุดมักอยู่ในแหล่งอังกฤษ — ใช้คู่กัน

---

## 🌏 แหล่งเรียนภาษาอังกฤษ (สำคัญมาก อย่าข้าม)

### คอร์สสั้นฟรี — DeepLearning.AI (ทำได้เร็ว ตรงประเด็น)
- ChatGPT Prompt Engineering for Developers (เฟส 1)
- Building Applications with Vector Databases (เฟส 2)
- LangChain for LLM Application Development / LangChain: Chat with Your Data (เฟส 3)
- Building and Evaluating Advanced RAG (เฟส 4–5)
- Functions, Tools and Agents with LangChain (เฟส 7)

### เอกสารทางการ (แหล่งที่แม่นและอัปเดตสุด — ฝึกอ่านให้คล่อง)
- **LangChain docs** / **LlamaIndex docs** — tutorial RAG ครบ
- **Anthropic docs** / **OpenAI docs** — การใช้ API, prompt engineering, tool use
- **Hugging Face docs** — โมเดล, embeddings, fine-tuning
- **RAGAS docs** — evaluation (เฟส 5)
- **Chroma / Qdrant / Weaviate / pgvector docs** — vector DB

### พื้นฐาน / เห็นภาพ
- **Andrew Ng** (Coursera: Machine Learning, Deep Learning Specialization) — ปูพื้นถ้าจะไป fine-tune
- **3Blue1Brown** (YouTube) — เวกเตอร์/linear algebra + ซีรีส์ Neural Network/Transformer (เห็นภาพสวยมาก)

### ความปลอดภัย (เฟส 6)
- **OWASP Top 10 for LLM Applications** — ความเสี่ยงด้านความปลอดภัยของแอป LLM

---

## 🧩 โปรเจคแนะนำแต่ละเฟส (เรียนแบบทำโปรเจค)

| เฟส | โปรเจคที่ควรทำให้จบ | ทักษะที่ได้ |
|-----|---------------------|-------------|
| 0 | สคริปต์เรียก API ฟรี ดึง JSON มา print / อ่าน CSV สรุปข้อมูล | Python, API, JSON, git |
| 1 | แชตบอท CLI ที่จำประวัติได้ + สลับใช้ cloud/Ollama | เรียก LLM, prompt, streaming |
| 2 | semantic search บนโน้ต/FAQ ตัวเอง (ยังไม่มี LLM ตอบ) | embedding, vector DB, similarity |
| 3 | **บอทตอบคำถามจาก PDF ของตัวเอง** (RAG ครบ pipeline) | RAG เต็มระบบ + citation |
| 4 | ปรับบอทเฟส 3 ด้วย reranker + hybrid + query transform | advanced retrieval |
| 5 | สร้าง test set + วัดด้วย RAGAS + ปรับ chunk size ด้วยข้อมูล | evaluation, data-driven tuning |
| 6 | deploy บอทเป็นเว็บ (FastAPI + UI + Docker) ให้คนอื่นใช้ + ต่อ observability | production, deploy, monitoring |
| 7 | agentic RAG (เลือกค้น/ใช้ tool เอง) หรือ fine-tune embedding | agent / fine-tuning (เลือก) |

**ไอเดียโปรเจคจริงที่ทำได้**: ผู้ช่วยตอบจากคู่มือพนักงาน/นโยบายบริษัท, บอทตอบจากเอกสารเรียน/สรุปวิชา, ค้นกฎหมาย/ระเบียบ, ผู้ช่วยตอบจาก documentation ของโปรเจคตัวเอง, ถาม-ตอบจากงานวิจัย PDF

> 💡 ทำ **โปรเจคเดียวต่อยอดข้ามเฟส** (เริ่มเฟส 3 แล้วพัฒนาต่อไปเรื่อยๆ) จะเห็นพัฒนาการชัดและได้ผลงานชิ้นใหญ่ไว้โชว์

---

## 📦 requirements.txt เริ่มต้น (ติดตั้งครบสำหรับเดินทั้ง roadmap)

```text
# --- core ---
python-dotenv
openai
anthropic
# --- embeddings & vector db ---
sentence-transformers
chromadb
faiss-cpu
# --- frameworks ---
langchain
langchain-community
langchain-openai
langchain-huggingface
langchain-chroma
langchain-text-splitters
llama-index
llama-index-embeddings-huggingface
# --- document loading ---
pypdf
# --- advanced retrieval ---
rank-bm25
langchain-experimental
# --- evaluation ---
ragas
datasets
# --- production ---
fastapi
uvicorn
streamlit
langfuse
# --- thai ---
pythainlp
# --- utils ---
tiktoken
numpy
```
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```
> ไม่ต้องติดตั้งทั้งหมดตั้งแต่วันแรก — ติดตั้งเพิ่มตามเฟสที่ถึง จะได้ไม่งงกับ error เยอะ

---

## 📖 คำศัพท์สำคัญ (Glossary)

| คำศัพท์ | ความหมายสั้นๆ |
|---------|----------------|
| LLM | โมเดลภาษาขนาดใหญ่ ทำนายคำถัดไป (Claude, GPT, Llama, Typhoon) |
| Token | หน่วยย่อยที่ LLM มองข้อความ (ไทยมักกิน token เยอะกว่าอังกฤษ) |
| Context window | จำนวน token สูงสุดที่ LLM รับได้ในครั้งเดียว |
| Hallucination | LLM ตอบผิดด้วยความมั่นใจ (เหตุผลหลักที่ต้องมี RAG) |
| Knowledge cutoff | วันสุดท้ายที่ข้อมูลของโมเดลอัปเดต |
| Prompt | คำสั่ง/ข้อความที่ส่งให้ LLM |
| Temperature | ความสุ่มของคำตอบ (ต่ำ=นิ่งแม่น, สูง=สร้างสรรค์) |
| RAG | Retrieval-Augmented Generation: ให้ LLM ตอบโดยอ้างอิงข้อมูลที่ค้นมา |
| Embedding | การแปลงข้อความเป็นเวกเตอร์ตัวเลขที่จับความหมาย |
| Vector / Dimension | ชุดตัวเลขแทนความหมาย / จำนวนตัวเลขในเวกเตอร์ |
| Cosine similarity | วัดความคล้ายของเวกเตอร์ด้วยมุม (ใกล้ 1 = คล้ายมาก) |
| Vector Database | ฐานข้อมูลเก็บเวกเตอร์ + ค้น top-k ที่ใกล้สุดเร็ว |
| Semantic search | ค้นด้วย "ความหมาย" แทนคำตรงเป๊ะ |
| Chunking | การแบ่งเอกสารยาวเป็นชิ้นเล็ก (สำคัญสุดต่อคุณภาพ) |
| Chunk overlap | ส่วนที่ chunk ซ้อนทับกัน กันประโยคขาด |
| Retrieval | ขั้นค้น chunk ที่เกี่ยวข้องกับคำถาม |
| Top-k | จำนวน chunk ที่ดึงมาใช้ |
| Indexing | กระบวนการเตรียมข้อมูล (chunk→embed→เก็บ) ล่วงหน้า |
| Reranking | จัดอันดับ chunk ใหม่ด้วยโมเดลแม่นกว่าหลังค้นกว้าง |
| Bi-encoder / Cross-encoder | embedding (เร็ว หยาบ) / โมเดลดูคู่คำถาม-เอกสาร (ช้า แม่น) |
| Hybrid search | ผสม semantic + keyword (BM25) |
| BM25 | อัลกอริทึมค้นคีย์เวิร์ดมาตรฐาน (sparse) |
| Query transformation | ปรับ/ขยายคำถามก่อนค้น (multi-query, HyDE, decomposition) |
| HyDE | แต่งคำตอบสมมติแล้วเอาไปค้น |
| Faithfulness | คำตอบยึด context จริงไหม (วัด hallucination) |
| Context precision/recall | chunk ที่ค้นมาเกี่ยวข้อง/ครบแค่ไหน |
| RAGAS | ไลบรารีวัดผล RAG |
| LLM-as-a-judge | ใช้ LLM ให้คะแนนคำตอบ |
| Agentic RAG | ระบบที่ LLM ตัดสินใจเองว่าจะค้นไหม/อย่างไร |
| Tool calling / Function calling | ให้ LLM เรียกใช้ฟังก์ชัน/เครื่องมือ |
| Fine-tuning | ปรับจูนโมเดลด้วยข้อมูลเฉพาะ (LoRA/QLoRA = ประหยัด) |
| GraphRAG | RAG ที่ใช้ knowledge graph เสริม |
| Observability / Tracing | การมองเห็นการทำงานภายในแต่ละคำขอ (Langfuse/LangSmith) |
| Prompt injection | การแอบใส่คำสั่งแย่งคุม LLM (ความเสี่ยงด้านความปลอดภัย) |
| PDPA | กฎหมายคุ้มครองข้อมูลส่วนบุคคลของไทย |
| Quantization | บีบโมเดลให้เล็กลงเพื่อรันบนเครื่องเล็ก |

---

## ⭐ กฎทอง 4 ข้อ (อ่านซ้ำเมื่อหลงทาง)

1. **Retrieval quality = ~80% ของคุณภาพ RAG** — ระบบตอบแย่ ให้กลับไปดู chunking + retrieval ก่อนโทษ LLM เสมอ
2. **อย่าข้าม Evaluation** — ถ้าวัดผลไม่เป็น คุณกำลังปรับระบบแบบสุ่ม เริ่มทำ eval ตั้งแต่ RAG ตัวแรก
3. **อย่าติดกับดักทฤษฎี** — ดูพอเข้าใจแล้วลงมือทำ ความรู้ที่ไม่ได้ใช้จะหายเร็ว ทุกเฟสต้องจบด้วยของที่รันได้
4. **เรียนแบบทำโปรเจค** — ทำโปรเจคเดียวต่อยอดข้ามเฟส จะเข้าใจลึกและได้ผลงานไว้โชว์

---

## 🗺️ สรุปลำดับการอ่านทั้งชุด

```
00 (ภาพรวม+วิธีเรียน) → 01 (พื้นฐาน) → 02 (LLM) → 03 (Embeddings/VectorDB)
→ 04 (RAG พื้นฐาน) → 05 (Advanced RAG) → 06 (Evaluation) → 07 (Production)
→ 08 (ต่อยอด ตามงาน)      ส่วน 09 (ไทย) อ่านคู่ไปตั้งแต่เฟส 2-3   |   10 (ไฟล์นี้) เปิดดูตลอดทาง
```

ขอให้สนุกกับการเรียนครับ 🚀 เริ่มจากไฟล์ `00` แล้วลงมือทำทีละเฟส
